@model IEnumerable<PastebookWebApplication.Models.PastebookModel>

TIMELINE
<table class="table">
    <tr>
        <th>PostId</th>
        <th>Content</th>
        <th>CreatedDate</th>
        <th>PosterId</th>
        <th>ProfileOwnerId</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>@item.PostEntity.ID</td>
            <td>@item.PostEntity.CONTENT</td>
            <td>@item.PostEntity.CREATED_DATE</td>
            <td>@item.PostEntity.POSTER_ID</td>
            <td>@item.PostEntity.PROFILE_OWNER_ID</td>
        </tr>
    }

</table>
